# Day8 Claude Design PPT 산출물 브리프

이 문서는 Claude Design에 붙여넣기 위한 PPT 제작 요청서입니다. 프로젝트 코드를 수정하기 위한 문서가 아니라, 발표 자료를 평가표 기준에 맞춰 정리하기 위한 문서입니다.

> 입력 근거: `PPT_EVIDENCE.md`(사실 근거) · `PPT_GAP_CHECK.md`(평가표 대비 갭)
> 라이브 데모: `https://sh.chris-cloud.com` · 코드: `https://github.com/quantumleapp/meeting-brain`
> **핵심:** 최고 배점은 **AI·클라우드 활용(30)** → AI·Ncloud·화면이 **하나의 흐름으로 연결**돼 보이게.

## 1. 프로젝트 요약

- 프로젝트명: **회의록 브레인 (Meeting Brain)**
- 한 줄 설명: 회의록을 붙여넣으면 AI가 **요약·결정·액션을 추출·저장**하고, 물어보면 과거 회의를 **RAG로 찾아 출처·과거 결정 교차점검과 함께** 답하며, 액션을 **담당자별 간트**로 추적하는 웹앱.
- 대상 사용자: 회의록을 남기고 검색하는 **팀원**(로그인 없음 · 공용 팀 보드).
- 해결하려는 문제:
  - ① 과거 회의의 **결정·맥락이 흩어져** 다시 찾기 어렵다 ("그때 가격 얼마로 정했지?")
  - ② 과거 결정과 **충돌·중복 논의**가 생긴다
  - ③ 회의에서 나온 **액션(담당자·기한)이 추적되지 않는다**
- 발표에서 보여줄 MVP 흐름:
  - **입력:** 회의 제목 · 날짜(`YYYY-MM-DD`) · 참석자(선택) · 원문 textarea, 또는 🎤 **음성**(CLOVA Speech STT)
  - **처리 중:** HyperCLOVA X가 요약·결정·액션 추출 → 원문 500자 청크 **임베딩(1024차원)** → **pgvector 저장** / 질문 시 "검색 중…" 로딩
  - **결과:** 답변 본문 + 핵심 강조 + **출처 칩(회의 제목·날짜)** + **⚠️ 과거 결정 교차점검** / 담당자별 **간트** / 연도별 **누적 대시보드**
  - **다시 시도 또는 기록:** 키 미설정·타임아웃 시 **명확한 오류 메시지 + 폴백**, "관련 회의록 없음·아직 액션 없음" **빈 상태 안내**; 액션 바 클릭으로 **예정→진행→완료** 상태 기록

## 2. Day7 Agent 기반 검토 역할

아래 역할의 관점을 PPT 검토에 반영합니다. (→ 충족 장표)

- 프로젝트 매니저: 목적·대상·기대 효과가 분명한가 → **S1·S2**
- 서비스 기획자: 사용자가 어떤 순서로 쓰는지 보이는가 → **S5(디자인 흐름)·S6(데모)**
- AI 기능 엔지니어: AI 입력·출력·**실패 상태**가 설명되는가 → **S4·S8**
- Ncloud 설계자: 사용한 Ncloud 서비스와 이유가 보이는가 → **S4**
- 인프라 팀장: 서버·배포·저장소·네트워크 구성이 간단히 보이는가 → **S4·S7**
- 디자인팀장: 화면이 발표용 장식이 아니라 **실제 앱 화면**처럼 보이는가 → **S6(목업 토큰 §4 하단)**
- 기능 검증팀장: 테스트·오류 수정·재시도 기록이 보이는가 → **S8**
- 보안팀장: API 키·개인정보·실제 금융/의료/학생 정보가 노출되지 않는가 → **전 장표(§4 민감정보 규칙)**
- 데이터 매니저: 샘플 데이터와 실제 데이터가 구분되는가 → **데모 데이터 "샘플" 표기**

## 3. 평가표 기준

총점은 100점입니다. PPT에서 아래 항목을 확인할 수 있어야 합니다.

| 평가 항목 | 배점 | PPT에 넣을 자료 | 담당 장표 |
|---|---:|---|---|
| 기획 및 목표 적합성 | 10 | 문제, 사용자, 기대 효과 | S2 |
| **AI·클라우드 활용 적절성** | **30** | AI 기능, Ncloud 서비스/인프라, **화면 흐름의 연결** | **S4·S5** |
| 구현 완성도 | 20 | 입력하면 결과가 나오는 실제 데모 흐름 | S3·S6·S7 |
| 품질 및 실용성 | 15 | 사용 흐름, 화면 가독성, 재시도 가능성 | S5·S6 |
| 문제 해결 노력 | 15 | 오류 원인, 수정 내용, 테스트 결과 | S8 |
| 문서화 완성도 | 10 | 목적, 기능, 시스템 구성, 사용 방법, 결과와 개선 방향 | S9·S10 |

> **배점 1순위 = AI·클라우드 활용(30).** AI 기능·Ncloud·화면이 따로 노는 나열이 아니라 **하나의 사용자 흐름**(입력→AI 추출→임베딩→pgvector→RAG→화면)으로 **연결**돼 보이도록 S4를 굵게.

## 4. 권장 장표 구성

> 장수에 억지로 맞추지 말 것. 아래는 권장(10장). 각 장표 = 제목 1 · 핵심 메시지 1 · 자료 2~4.

1. **표지** — 메시지 "회의록을 회사의 기억으로." / 자료: 프로젝트명·팀(이름)·한 줄 설명·라이브 URL/QR. [전반]
2. **문제와 사용자** — 메시지 "과거 회의 결정이 흩어져 다시 찾기 어렵다." / 자료: 문제 3 + 대상(팀원·로그인 없음·공용 보드). [기획 10]
3. **MVP 범위** — 메시지 "붙여넣으면 정리, 물으면 출처와 함께, 액션은 간트." / 자료: 입력→처리→결과 플로우, 실제 동작 기능 목록(엔드포인트). [구현 20]
4. **AI·Ncloud 구성 ★** — 메시지 "AI·클라우드·화면이 하나의 흐름으로 연결." / 자료: ①AI 입출력 표(§5-1) ②Ncloud 아키텍처(§5-2, **실제 docker pgvector**) ③데이터 흐름 화살표. [AI·클라우드 30 — 최고]
5. **디자인 흐름** — 메시지 "입력 → 처리 중 → 결과 → 다시 시도." / 자료: 4단계 상태(로딩 "검색 중…", 오류 메시지, 빈 상태 포함). [품질 15 / AI·클라우드 30]
6. **화면 데모** — 메시지 "실제 앱 화면 그대로." / 자료: 입력 화면, ①검색(출처 칩·⚠️교차점검), ②액션 보드(간트), ③대시보드 목업. [구현 20 / 품질 15]
7. **구현 자료** — 메시지 "실제로 동작·배포됨." / 자료: 폴더 구조, 엔드포인트 목록, 스모크/실행 화면(마스킹), 배포(라이브·HTTPS·GitHub). [구현 20 / AI·클라우드 30]
8. **테스트와 오류 해결 ★** — 메시지 "문제를 확인하고 기록·해결." / 자료: pytest 19 통과 + 스모크 7/7, 오류→원인→해결 표(§6 본문). [문제 해결 15]
9. **한계와 개선 방향** — 메시지 "지금 못한 것과 다음에 할 것." / 자료: 범위 밖 로드맵(음성 STT 확대·외부 연동(Slack·캘린더)·인증/권한). [문서화 10]
10. **마무리** — 메시지 "무엇을 만들었나 + 산출물." / 자료: 완주 요약(로컬→테스트→배포→공개) + 제출 산출물 목록(코드·README·SERVER_RUNBOOK·EVIDENCE·라이브 URL). [문서화 10]

### 4-1. 화면 목업용 디자인 토큰 (S5·S6이 실제 앱처럼 보이게)
- 다크 테마: 배경 `#0e0e11`, 패널 `#1a1a1f`, 얇은 헤어라인, 둥근 카드
- 포인트: 블루 `#5b8cff`, 그린 `#34d399`, 경고 앰버 `#f5b34a`
- 액션 상태색: 예정=회색 `#6b7280` / 진행=파랑 / 완료=초록
- 텍스트: 제목 `#f4f4f6`, 본문 `#c4c6cf` / 한글 **Pretendard** / **날짜·코드·수치는 모노스페이스**
- 출처·태그 = **둥근 칩** / 차트는 **단순 바**(무거운 차트 라이브러리 느낌 금지)
- 금지: 과장 카피·버즈워드, 스톡 사진, 이모지 남발, 화려한 애니메이션, 저대비 텍스트

### 4-2. 민감정보·데이터 표기 규칙 (보안팀장·데이터 매니저)
- 🔴 절대 금지: `.env` 값(CLOVA API 키 `nv-…`, Speech 키, Invoke URL, Embed/Chat URL), **서버 공인 IP**, SSH `.pem`, **root 비밀번호**
- 🟡 주의: DB 계정/비번·콘솔/터미널 캡처는 **마스킹**. 도메인은 데모 URL로만(강사 제공)
- 🟢 안전: 모든 데모 데이터는 **합성/샘플**(실명·연락처·계좌·실거래·의료/학생 정보 금지). 인물명은 가상(김하늘·이도현 등). 화면에 "샘플 데이터" 표기

### 4-3. AI·Ncloud·화면 상세 (S4·S5·S6 디자이너용)

**5-1. AI 입력/출력**
| AI 기능 | 입력 | 출력 |
|---|---|---|
| 회의록 정리 | 회의록 원문 | `{요약, 결정[], 액션[담당자·시작·마감]}` |
| 텍스트 임베딩 | 500자 청크(겹침 50) | **1024차원 벡터** |
| RAG 검색 | 질문 임베딩 | 관련 회의 **top-5**(코사인) |
| RAG 답변 | 질문 + 검색 발췌 + 과거 결정 | 답변 + 출처(제목·날짜) |
| ⚠️ 교차점검 | 검색된 회의 발췌들 | 과거↔현재 결정 차이(없으면 표시 안 함) |
| 음성 인식(STT) | 오디오 | 텍스트 |
- 강조: **검색된 근거에만 의존**(없으면 "모른다"), **환각 방지**(검색된 회의 제목만 인용) = 실패/안전 상태.

**5-2. Ncloud 아키텍처 (실제 구성)**
```
[브라우저] ─HTTPS(sh.chris-cloud.com)→ [Caddy: 자동 TLS, 80→443]
   → [FastAPI app] ─(docker 내부망)→ [pgvector (docker, pg16)]
        ├─HTTPS→ CLOVA Studio (임베딩 1024 + HyperCLOVA X 답변)
        └─HTTPS→ CLOVA Speech (장문 STT)
   배포: Ncloud Server(Ubuntu 22.04) 1대 · docker compose(app+db) · ACG 80/443/8000, 5432 비공개
```
> ⚠️ **실제 = 서버 내 docker pgvector** (매니지드 Cloud DB 아님 — 그건 대안/로드맵으로만 언급).

**5-3. 화면 흐름** — 입력 → ① 검색 → ② 액션 보드 → ③ 대시보드
- 입력: 제목·날짜·참석자·원문 + 🎤 / 로딩·오류·빈 상태
- ① 검색: 검색창 → 답변 카드(본문 + 출처 칩 + ⚠️ 교차점검)
- ② 보드: 담당자(행)×날짜(열) 간트, 월 고정폭·가로 스크롤, 상태색·진행률, 바 클릭 토글
- ③ 대시보드: 연도별 막대 + "임베딩→Vector DB→RAG" 캡션

## 5. Claude Design 요청문

아래 요청문 뒤에 이 문서 전체를 붙여넣습니다.

```text
아래 프로젝트 브리프를 바탕으로 16:9 발표용 PPT 초안을 만들어줘.

요구사항:
- 장표 수를 정해진 장수에 억지로 맞추지 말고, 평가표와 데모 흐름이 빠지지 않게 구성한다.
- 평가표 6개 항목이 어느 장표와 연결되는지 보이게 한다. (특히 최고 배점인 AI·클라우드 활용 30점은 AI 기능·Ncloud·화면 흐름이 하나로 연결된 구조도 장표로 강조한다.)
- 화면은 마케팅 랜딩페이지처럼 만들지 말고, 실제 앱 데모 화면처럼 정보 밀도를 유지한다. (다크 테마: 배경 #0e0e11/패널 #1a1a1f, 포인트 블루 #5b8cff·그린 #34d399, 상태색 예정=회색/진행=파랑/완료=초록, 날짜·코드는 모노스페이스, 출처는 둥근 칩.)
- 각 장표는 제목 1개, 핵심 메시지 1개, 자료 2~4개만 둔다.
- AI 기능, Ncloud 서비스, 화면 디자인 흐름은 구조도 장표에서 함께 보여준다. (Ncloud 구성은 실제 = 서버 내 docker pgvector. 매니지드 Cloud DB 아님.)
- 테스트와 오류 해결 장표를 포함한다. (단위 테스트 19 통과 + 스모크 7/7, 오류→원인→해결 표.)
- 실제 API 키, 서버 접속 정보(공인 IP·SSH 키·비밀번호), 개인정보는 쓰지 않는다.
- 모든 데이터는 샘플/데모용으로 표시한다. (인물명은 가상.)
- 발표자가 3분 안에 설명할 수 있게 장표별 발표 메모를 함께 작성한다.
```

## 6. Claude Design 결과 검수 기준

- 첫 3장 안에 "무엇을 만들었는지"가 보이는가
- AI, Ncloud, 화면 디자인이 **하나의 사용자 흐름**으로 설명되는가 (← 최고 배점 30점)
- 실제 입력과 결과 화면이 보이는가
- 오류 수정 또는 테스트 결과가 빠지지 않았는가
- 장표가 보기만 좋고 평가표 확인 자료가 빠져 있지 않은가
- 작은 화면에서도 표와 코드가 읽히는가
- 제출 전 README, 실행 방법, 개선 방향이 정리되어 있는가
- (보안) 키·서버 접속 정보·개인정보가 화면/목업/캡처에 없는가
